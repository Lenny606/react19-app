// PROJECTID
// DATABSEID
// COLLECTIONID

// const client = new Client().setEndpoint('https://cloud.appwrite.io/v1').setProject(PROJECTID);
// const dtb = new Databases(client)

export const updateSearchCount = (search, movie) => {
    // 1. check if search exists in dtb
    // 2. if it does, update the count
    // 3. if it doesn't, create a new record
}

export const getTrendidng = () => {
    // 1. list and sort by count}
}